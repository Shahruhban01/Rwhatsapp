rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE: Allow all reads/writes when using the emulator.
    // In production, replace this with proper Firebase Auth-based rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
